wink.math = {
	round: function(n) {
		return 0;
	}
};

wink.math.Matrix = function(properties)
{
	this.uId = 0;
};

wink.fx.addClass = function(node, classStr) 
{
	return "addClass IE Impl é";
} ;